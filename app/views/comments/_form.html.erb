<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :body %>
    <%= f.association :rally %>
  </div>

  <div class="form-actions">
    <%= f.submit "Save" %>
  </div>
<% end %>

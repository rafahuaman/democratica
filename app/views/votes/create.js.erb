<% if @votable.vote_type == "Rally" %>
  $($('#score-grid-rally-<%= @votable.id.to_s %>')).html("<%= escape_javascript(render('votes/vote_form')) %>")
<% else %>
  $($('#score-grid-comment-<%= @votable.id.to_s %>')).html("<%= escape_javascript(render('votes/comment_voting_grid')) %>")
<% end %>